import 'package:fjorubordid_app_final_version/screens/drink_screen.dart';
import 'package:fjorubordid_app_final_version/screens/food_screen.dart';
import 'package:fjorubordid_app_final_version/screens/order_screen.dart';
import 'package:flutter/material.dart';
import 'package:flutter/services.dart';
/*
void main() {
  WidgetsFlutterBinding.ensureInitialized();
  SystemChrome.setPreferredOrientations([
    DeviceOrientation.portraitUp,
    DeviceOrientation.portraitDown,
  ]).then((value) => runApp(const MyApp()));
  runApp(const MyApp());
}

class MyApp extends StatelessWidget {
  const MyApp({super.key});
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
        debugShowCheckedModeBanner: false,
        /*title: 'Fjöruborðið App',
        theme: ThemeData(
          primaryColor: primary,
          textTheme: GoogleFonts.latoTextTheme(
            Theme.of(context).textTheme,
          ),
        ),*/
        initialRoute: "screens/food",
        routes: {
          "screens/foodscreen": (context) => const FoodScreen(),
          "screens/drinkscreen": (context) => const DrinkScreen(),
          "screens/orderscreen": (context) => const OrderScreen(),
        });
  }
}*/
